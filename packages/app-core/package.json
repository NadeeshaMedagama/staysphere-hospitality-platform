{
  "name": "@staysphere/app-core",
  "version": "1.0.0",
  "private": true,
  "description": "Gateway client, session handling and auth plumbing shared by the guest site, the operations console and the staff app.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "rimraf .turbo dist",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:cov": "vitest run --coverage --passWithNoTests"
  },
  "dependencies": {
    "@staysphere/contracts": "workspace:*"
  },
  "peerDependencies": {
    "next": "^15.1.4"
  },
  "devDependencies": {
    "@staysphere/eslint-config": "workspace:*",
    "@staysphere/typescript-config": "workspace:*",
    "@types/node": "^22.10.5",
    "eslint": "^9.17.0",
    "next": "^15.1.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
