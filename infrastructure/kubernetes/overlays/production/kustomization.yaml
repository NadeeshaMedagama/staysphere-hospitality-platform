apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staysphere

resources:
  - ../../base

patches:
  - target:
      kind: ConfigMap
      name: staysphere-config
    patch: |-
      - op: replace
        path: /data/LOG_LEVEL
        value: info
